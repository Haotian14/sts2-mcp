# 默认：仓库内统一 LF，检出时按需转换
* text=auto eol=lf

# ---------------------------------------------------------------------------
#  Windows 批处理必须是 CRLF —— 这不是风格问题
#
#  cmd.exe 解析 LF 换行的 .cmd/.bat 时会出错，多行结构（if / for）尤其明显。
#  本项目已实测踩过：build.bat 因 LF 导致 for 循环解析失败，报出
#  "'tokens' is not recognized as an internal or external command"
#  这类与真实原因毫无关联的错误信息。
#
#  scripts/launch-steam.cmd 是 Steam 启动游戏的入口，一旦换行符被转换
#  即彻底失效，且症状极难联想到换行符。
# ---------------------------------------------------------------------------
*.cmd  text eol=crlf
*.bat  text eol=crlf

# PowerShell 脚本同样使用 CRLF。
# 另注意：含非 ASCII 字符的 .ps1 必须保存为 UTF-8 with BOM ——
# Windows PowerShell 5.1 会按系统代码页读取无 BOM 文件，中文将变为乱码
# 并引发语法错误。BOM 属文件内容，git 不会改动，但新增脚本时须自行保证。
*.ps1  text eol=crlf
*.psm1 text eol=crlf

# 项目文件遵循 Windows 工具链惯例
*.csproj text eol=crlf
*.sln    text eol=crlf

# 二进制，禁止任何转换
*.dll binary
*.pdb binary
*.exe binary
*.pck binary
*.png binary
